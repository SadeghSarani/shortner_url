# Shortner Url

a laravel project + docker


steps up and using :

open project and run this command

docker compose up -d --build

and then project is run and after on this

in url /docs/api is view documention project

and port using for project is localhost:2110 and phpmyadmin is localhost:2686

and for run test go to container project and then run php artisan test

enjoy!
